# Copy to .env.local and fill in once the backend is deployed (Person 1
# shares the API Gateway URL). Leave VITE_API_BASE_URL unset / VITE_USE_MOCKS
# true to develop against the local mock fixtures.

VITE_USE_MOCKS=true
VITE_API_BASE_URL=
VITE_CAMPUS_DEFAULT_LAT=12.9716
VITE_CAMPUS_DEFAULT_LON=77.5946
